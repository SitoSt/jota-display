# Variables de entorno locales (deploy desde el Mac)
# Copia este fichero como .env.local y rellena los valores reales.
# .env.local NUNCA se versiona — contiene credenciales de acceso.

# Credenciales SSH al teléfono (Termux)
PHONE_HOST=u0_a161@192.168.1.129
PHONE_PORT=8022
PHONE_PASS=changeme

# Directorio de destino en el teléfono
PHONE_DIR=/data/data/com.termux/files/home/jota-display
